import 'package:flutter/material.dart';

class CheckGrammerScreen extends StatelessWidget {
  const CheckGrammerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
